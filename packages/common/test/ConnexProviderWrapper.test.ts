import { useEnvironment } from "./helpers/useEnvironment";

describe("createProvider", function () {
    describe("without url", function () {
        useEnvironment("invalid-no-url");
        it("should fail", function () {
            
        })
    })
});